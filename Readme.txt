How to Run:
1. Compile the Java files using `javac`.
2. Run the programs using `java`.
3. For Task #2, ensure `input.txt` is in the same directory.
   a. Each line should contain {Pid Arrival_Time Burst_Time Priority}, separated by spaces (ignore brackets)
4. Follow terminal instructions to choose algorithms and input time quantum.